numpy

# NOTE — two dependencies deliberately do NOT come from PyPI:
#
# gtsam: this pipeline needs the custom factors (DD PR/CP, Doppler,
# SD Doppler, NHC), which the stock PyPI 'gtsam' wheel lacks. Install
# the prebuilt wheel from inuex35/gtsam (branch custom/develop,
# re-merged with upstream weekly):
#   gh release download custom-wheels-latest -R inuex35/gtsam -p '*cp312*manylinux*' -D wheels
#   pip install "$(ls wheels/*.whl | sort | tail -1)"
#
# cssrlib: upstream lacks prepare_double_difference_measurements /
# base_process_dd_only (the layered DD-only RTK core this pipeline
# drives). Pinned to a commit for reproducibility:
cssrlib @ git+https://github.com/inuex35/cssrlib.git@750a48e2ba7a2322d8a46cb8caeb7436f21ae66e
